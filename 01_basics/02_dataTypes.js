"use strict"; //treat all JS code with newer version

// alert( 3+3) // we are using nodejs and not browser, so it throws error

let name = "kittu" //string
let age = 18 //numeric
let isloggedIn = false //boolean

/*ecma -> since different browsers were creating 
diff methodologies for their js, ecma is an organizaton
that standardizeed the approac */


console.log(typeof age)


